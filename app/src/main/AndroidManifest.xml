<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobilessu">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".session_page.SessionActivity"
            android:parentActivityName=".group_page.ActivityGroup">
        </activity>
        <activity
            android:name=".schedule_page.ScheduleActivity"
            android:parentActivityName=".group_page.ActivityGroup" />
        <activity android:name=".group_page.ActivityGroup" />
        <activity android:name=".choose_course.ActivityСourse" />
        <activity android:name=".choose_day_evening.ActivityDayEvening" />
        <activity android:name=".menu_page.ActivityMenu" />
        <activity android:name=".student_faculty_page.ActivityStudentFaculty" />
        <activity
            android:name=".teacher_page.ActivityFindTeacher"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".teacher_or_student_page.ActivityTeacherOrStudent" />

        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".news_page.ActivityNews"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>